EMX Soul Arena v18 — No Sound Button Fix

Fixes:
- Removes the bottom "Tap to enable game sounds" / Activate Sounds button.
- Keeps sound effects working from normal taps and attacks.
- Keeps v17 working button router.
- Adds CSS/JS fallback so the sound button cannot block the UI.
- Adds cache busting to v18.

Install:
Copy everything inside this folder to your repo root, commit, push, wait for Vercel, then test in Safari.
